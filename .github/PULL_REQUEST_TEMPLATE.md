## What this changes

<!-- One or two sentences. If it fixes an issue, link it: Fixes #123 -->

## Why

<!-- What was wrong, or what was missing. The reasoning matters more than the diff. -->

## Checklist

- [ ] `npm test` passes
- [ ] `npm run typecheck` passes
- [ ] Added or updated a test — required for anything touching `guard/` or `map/actions.ts`
- [ ] Added a changeset (`npm run changeset`) if a published package changed
- [ ] `npm run size` still passes, if the browser package changed

## Anything reviewers should look at closely

<!-- A trade-off you were unsure about, a case you deliberately did not handle, a
     performance or security implication. Say so here rather than hoping it is noticed. -->
